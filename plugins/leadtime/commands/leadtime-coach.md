---
description: Ongoing Leadtime help after setup: configure modules, answer questions, import data, and build integrations.
---

Start the **`leadtime-coach`** skill for an **existing** Leadtime workspace.

Connect through Leadtime MCP, ask for the concrete goal when it is not already clear, then follow the `leadtime-coach` `SKILL.md`: answer knowledge questions through the help center first, perform writes through `list-actions` -> `action-details` -> `execute-action` with confirmation, then provide a review checkpoint and offer repairs. Handle one step at a time and split larger work into phases.
